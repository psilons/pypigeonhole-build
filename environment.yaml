name: py390_bld

channels:
    - defaults

dependencies:
    - python>=3.6
    - pip
    - coverage==5.3
    - conda-build
    - anaconda-client
    - pip:
        - pipdeptree
        - coverage-badge
        - twine
